---
description: 技术选型预警规则 - 避免反复打补丁，必须停下分析根因
alwaysApply: true
enabled: true
---

# 技术选型预警规则

## 🔴 最高优先级规则

### 核心原则

**当一个问题需要第二次修复时，必须停下来分析根因，而不是继续打补丁。**

---

## 📍 触发条件

以下任一情况出现时，**立即停止当前修复方向**，转为根因分析：

1. **同一类错误出现第二次** — 说明第一次修复不彻底
2. **需要加"防御层"来防止某个问题** — 说明根源没解决
3. **一个 hack 引发了新问题，需要另一个 hack 来修** — 说明方向错了
4. **需要用全局变量、monkey-patch 等非常规手段** — 说明架构有问题
5. **同一工具在不同场景下都要特殊处理** — 说明该工具与技术栈不兼容

---

## 🚫 禁止的行为

- ❌ 在同一个问题上连续加超过 **1 层** "防御/保障"
- ❌ 用 hack 修 hack
- ❌ 遇到不兼容时不告知用户，默默加 workaround
- ❌ 升级/降级依赖时不检查与现有技术栈的兼容性
- ❌ 引入新依赖时不检查其维护状态和与当前栈的兼容性

---

## ✅ 必须执行的动作

当触发条件出现时，**必须停下来执行以下步骤**：

### 第一步：向用户报告技术风险

```
⚠️ 技术选型预警：
- 问题：[具体描述问题]
- 已尝试的修复：[列出已尝试的 hack/workaround]
- 根因分析：[为什么问题反复出现]
- 当前状态：[是否工作，但存在风险]
```

### 第二步：列出根本解决方案（2-3 个）

```
方案 A：[优点/缺点]
方案 B：[优点/缺点]
方案 C：[优点/缺点]
```

### 第三步：让用户决定

**不要自己决定继续打补丁**。把选择权交给用户。

---

## 🔧 技术选型检查清单

在以下情况时**必须执行**检查清单：

1. **引入新依赖前**
2. **升级工具版本前**
3. **选择技术方案时**

### 检查项

- [ ] 该工具/库是否与当前技术栈的**模块格式兼容**（CJS vs ESM）？
- [ ] 该工具是否有与现有技术的**已知兼容性问题**？
- [ ] 主要依赖的 **peer dependency 版本**是否与现有栈匹配？
- [ ] 该工具是否在**积极维护**？
- [ ] 官方是否已标记为 **deprecated** 或推荐迁移？
- [ ] 如有不兼容风险，**是否已告知用户**？

### 已废弃技术黑名单（禁止在本项目选用）

| 技术 | 替代方案 | 状态 |
|------|---------|------|
| Create React App (CRA) | Vite / Next.js | ❌ 停止开发 |
| Moment.js | Day.js / date-fns | ❌ 不推荐 |
| Request (HTTP) | Axios / fetch / undici | ❌ 停止维护 |
| TSLint | ESLint | ❌ 已迁移 |

---

## CourtSimulator 特定风险点

> CourtSimulator 是 AI 驱动的法庭模拟器，以下是本项目需要特别关注的技术风险：

### 1. AI/LLM 集成风险
- **流式输出（Streaming）**：确认前端框架支持 SSE / WebSocket 的流式渲染
- **长对话上下文**：提前设计对话历史的裁剪策略，避免 token 超限
- **响应延迟**：UI 必须支持 loading/typing 状态，避免用户感知到卡顿

### 2. 状态管理复杂度
- 法庭场景涉及多角色（法官/律师/证人/陪审团）、多轮对话、案件状态流转
- 如果状态管理方案第二次出现"数据不同步"问题 → 立刻停下，评估是否需要引入更强的状态管理（如 Zustand、Redux Toolkit）

### 3. 构建工具版本
- **Vite 版本**：如遇到 CJS/ESM 兼容性问题，优先降级到 Vite 5.x，不要反复打补丁（历史教训：Vite 6 对 CJS React 处理有 bug）

---

## 💡 判断流程

```
遇到问题时：
1. 这是第几次修这个问题？
   ├─ 第 1 次 → 尝试修复，记录方案
   └─ 第 2 次+ → 🛑 停下，分析根因

2. 修复时是否在加"保护层"？
   ├─ 是 → 🛑 停下，这说明根源没解决
   └─ 否 → 继续修复

3. 修复方案是否超过"正常范围"？
   ├─ 是（全局变量、脚本验证等）→ 🛑 停下
   └─ 否 → 继续修复
```

---

## ✅ 检查清单

在完成修复前，必须确认：

- [ ] 这是同一问题的第几次修复？
- [ ] 是否在加"防御层"或 hack？
- [ ] 根因是什么？
- [ ] 已列出 2-3 个根本方案吗？
- [ ] 用户已知晓技术风险吗？

---

**最后更新**：2026-03-23
**优先级**：🔴 最高（强制执行）
**适用范围**：CourtSimulator 所有技术决策
